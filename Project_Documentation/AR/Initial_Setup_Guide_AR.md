# 🚀 دليل الإعداد الأولي

## توحيد التكوين وإنشاء أول مدير نظام

يرشدك هذا الدليل خلال عملية الإعداد الكاملة لنظام إدارة العقارات، بما في ذلك تكوين البيئة وإنشاء أول مستخدم مدير.

---

## جدول المحتويات

1. [نظرة عامة على بنية النظام](#نظرة-عامة-على-بنية-النظام)
2. [تكوين البيئة](#تكوين-البيئة)
3. [إنشاء أول مدير نظام](#إنشاء-أول-مدير-نظام)
4. [تشغيل التطبيق](#تشغيل-التطبيق)
5. [التحقق والاختبار](#التحقق-والاختبار)
6. [حل المشاكل](#حل-المشاكل)

---

## نظرة عامة على بنية النظام

### مصدر واحد للحقيقة في التكوين

يستخدم التطبيق الآن **نظام تكوين مركزي** بالبنية التالية:

```
/workspace
├── api/
│   ├── .env                      # التكوين الافتراضي
│   ├── .env.development          # تكوين بيئة التطوير
│   ├── .env.production           # تكوين بيئة الإنتاج
│   └── src/
│       └── config/
│           └── app.config.ts     # خدمة التكوين المُحكّمة
├── ecosystem.config.js           # PM2 (يدعم البيئات المختلفة)
└── Web/
    └── package.json              # الواجهة الأمامية
```

### المبادئ الأساسية

1. **لا قيم مُدمجة**: جميع المنافذ والروابط والأسرار تُقرأ من ملفات البيئة
2. **دعم البيئات المختلفة**: إعدادات مختلفة للتطوير والإنتاج
3. **آمن من الأخطاء**: التحقق من صحة التكوين عبر TypeScript
4. **تكامل PM2**: مدير العمليات يتحكم في البيئة المستخدمة

---

## تكوين البيئة

### الخطوة 1: تكوين ملفات بيئة Backend

يستخدم الـ Backend ملفات تكوين خاصة بالبيئة في مجلد `/api`.

#### بيئة التطوير (Development)

عدّل الملف `/api/.env.development`:

```bash
# ═══════════════════════════════════════════════════════════════
# تكوين بيئة التطوير
# ═══════════════════════════════════════════════════════════════

NODE_ENV=development

# منافذ التطبيق
PORT=3001
FRONTEND_PORT=8088

# إعدادات Supabase
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_actual_service_role_key

# إعدادات JWT
JWT_SECRET=your-development-jwt-secret-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS - الأصول المسموح بها
ALLOWED_ORIGINS=http://localhost:8088,http://127.0.0.1:8088,http://localhost:3000,http://127.0.0.1:3000
```

#### بيئة الإنتاج (Production)

عدّل الملف `/api/.env.production`:

```bash
# ═══════════════════════════════════════════════════════════════
# تكوين بيئة الإنتاج
# ═══════════════════════════════════════════════════════════════

NODE_ENV=production

# منافذ التطبيق
PORT=3002
FRONTEND_PORT=3000

# إعدادات Supabase
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_actual_service_role_key

# إعدادات JWT (غيّر هذه القيم!)
JWT_SECRET=استخدم_سر_قوي_عشوائي_32_حرف_على_الأقل
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS - الأصول المسموح بها
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com
```

### الخطوة 2: الحصول على بيانات Supabase

1. اذهب إلى لوحة تحكم Supabase: https://app.supabase.com
2. انتقل إلى **Settings** → **API**
3. انسخ القيم التالية:
   - **Project URL** → استخدمه كـ `SUPABASE_URL`
   - **service_role secret** → استخدمه كـ `SUPABASE_SERVICE_ROLE_KEY`
   
   ⚠️ **مهم جداً**: مفتاح service_role له صلاحيات كاملة. لا تشاركه أبداً!

### الخطوة 3: إنشاء JWT Secret

للإنتاج، أنشئ JWT secret قوي وعشوائي:

```bash
# على Linux/Mac
openssl rand -base64 32

# أو استخدم Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

انسخ الناتج واستخدمه كـ `JWT_SECRET` في `.env.production`.

---

## إنشاء أول مدير نظام

### المتطلبات الأساسية

1. ✅ تكوين ملفات البيئة (`/api/.env.development` أو `.env.production`)
2. ✅ اتصال Supabase يعمل
3. ✅ الـ Backend مُجمّع (`npm run build` في مجلد `/api`)

### عملية إنشاء المدير خطوة بخطوة

#### 1. الاتصال بالخادم

```bash
# الاتصال بالخادم عبر SSH
ssh user@your-server-ip

# الانتقال إلى مجلد المشروع
cd /workspace/api
```

#### 2. التأكد من تثبيت الحزم

```bash
npm install
```

#### 3. تشغيل أداة إنشاء المدير

```bash
npm run seed:superadmin -- \
  --email="admin@yourdomain.com" \
  --password="كلمة_مرور_قوية_جداً123!" \
  --name="مدير النظام" \
  --phone="+966500000000"
```

**المعاملات المطلوبة:**
- `--email`: البريد الإلكتروني للمدير (يجب أن يكون فريداً)
- `--password`: كلمة مرور قوية (8 أحرف على الأقل، يُنصح بـ 12+)

**المعاملات الاختيارية:**
- `--name`: الاسم الكامل (افتراضي: "System Administrator")
- `--phone`: رقم الهاتف (افتراضي: "+966500000000")

#### 4. ناتج الأمر

عند النجاح، سترى:

```
═══════════════════════════════════════════════════════════
  🔐 SuperAdmin User Seeder
═══════════════════════════════════════════════════════════

🔗 Connecting to Supabase...
🔍 Checking if user already exists...
🏢 Checking for system office...
✅ System office found
🔒 Hashing password...
👤 Creating superadmin user...

═══════════════════════════════════════════════════════════
  ✅ SuperAdmin User Created Successfully!
═══════════════════════════════════════════════════════════
User Details:
  ID:        xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
  Email:     admin@yourdomain.com
  Name:      مدير النظام
  Phone:     +966500000000
  Role:      system_admin
  Office ID: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
  Status:    active
═══════════════════════════════════════════════════════════

🎉 You can now login with these credentials!
   Email:    admin@yourdomain.com
   Password: (كلمة المرور التي أدخلتها)
```

#### 5. أفضل الممارسات الأمنية

⚠️ **مهم**:
- احفظ بيانات المدير في مدير كلمات مرور آمن
- لا تحفظ كلمات المرور في نظام التحكم بالإصدارات (Git)
- غيّر كلمة المرور الافتراضية بعد أول تسجيل دخول
- فعّل المصادقة الثنائية (2FA) إن أمكن

---

## تشغيل التطبيق

### باستخدام PM2 (موصى به للإنتاج)

ملف `ecosystem.config.js` الآن يدعم التكوين حسب البيئة.

#### تشغيل بيئة التطوير

```bash
# تشغيل API و Frontend في وضع التطوير
pm2 start ecosystem.config.js --only dev-api,dev-frontend

# أو تشغيلهم بشكل منفصل
pm2 start ecosystem.config.js --only dev-api
pm2 start ecosystem.config.js --only dev-frontend
```

**تكوين التطوير:**
- Backend API: المنفذ `3001` (من `.env.development`)
- Frontend: المنفذ `8088` (من PM2)
- NODE_ENV: `development`

#### تشغيل بيئة الإنتاج

```bash
# تشغيل API و Frontend في وضع الإنتاج
pm2 start ecosystem.config.js --only prod-api,prod-frontend

# أو تشغيلهم بشكل منفصل
pm2 start ecosystem.config.js --only prod-api
pm2 start ecosystem.config.js --only prod-frontend
```

**تكوين الإنتاج:**
- Backend API: المنفذ `3002` (من `.env.production`)
- Frontend: المنفذ `3000` (من PM2)
- NODE_ENV: `production`

#### إدارة عمليات PM2

```bash
# عرض جميع العمليات
pm2 list

# عرض السجلات
pm2 logs

# إعادة تشغيل جميع العمليات
pm2 restart all

# إيقاف جميع العمليات
pm2 stop all

# حفظ تكوين PM2
pm2 save

# إعداد PM2 للتشغيل التلقائي عند بدء النظام
pm2 startup
```

### التشغيل اليدوي (للتطوير فقط)

```bash
# Terminal 1: Backend
cd /workspace/api
NODE_ENV=development npm run start:prod

# Terminal 2: Frontend
cd /workspace/Web
PORT=8088 npm start
```

---

## التحقق والاختبار

### 1. فحص صحة Backend

```bash
# التطوير
curl http://localhost:3001/health

# الإنتاج
curl http://localhost:3002/health
```

النتيجة المتوقعة:
```json
{
  "status": "ok",
  "timestamp": "2025-11-11T12:00:00.000Z",
  "environment": "development",
  "port": 3001
}
```

### 2. اختبار تسجيل دخول المدير

#### باستخدام cURL

```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@yourdomain.com",
    "password": "كلمة_مرور_قوية_جداً123!"
  }'
```

النتيجة المتوقعة:
```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "...",
    "email": "admin@yourdomain.com",
    "role": "system_admin",
    "office_id": "..."
  }
}
```

#### باستخدام الواجهة الأمامية

1. افتح المتصفح وانتقل إلى:
   - التطوير: `http://localhost:8088`
   - الإنتاج: `http://your-domain.com`

2. أدخل بيانات المدير
3. يجب أن يتم توجيهك إلى لوحة التحكم

### 3. التحقق من تحميل التكوين

افحص سجلات PM2 للتأكد من تحميل البيئة الصحيحة:

```bash
pm2 logs dev-api --lines 50
```

ابحث عن رسالة البدء:
```
═══════════════════════════════════════════════════
🚀 Backend is running!
📍 Local URL:    http://localhost:3001
📚 Environment:  development
🔒 CORS Origins: http://localhost:8088, ...
📖 Swagger UI:   http://localhost:3001/api/docs
═══════════════════════════════════════════════════
```

---

## حل المشاكل

### مشكلة: "SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set"

**الحل:**
1. تحقق من وجود `.env.development` أو `.env.production` في مجلد `/api`
2. تأكد من أن الملف يحتوي على بيانات Supabase صحيحة
3. تأكد من إعادة تشغيل PM2 بعد تغيير ملفات .env: `pm2 restart all`

### مشكلة: "User with email already exists"

**الحل:**
- البريد الإلكتروني مسجل بالفعل في قاعدة البيانات
- إما استخدم بريد إلكتروني مختلف أو احذف المستخدم الموجود يدوياً:
  ```sql
  DELETE FROM users WHERE email = 'admin@yourdomain.com';
  ```

### مشكلة: "Port already in use"

**الحل:**
1. تحقق من العملية التي تستخدم المنفذ:
   ```bash
   sudo lsof -i :3001  # أو :3002 للإنتاج
   ```
2. أوقف العملية المتعارضة أو غيّر PORT في `.env.development`/`.env.production`
3. أعد تشغيل PM2: `pm2 restart all`

### مشكلة: "Cannot connect to Supabase"

**الحل:**
1. تحقق من صحة رابط Supabase (يجب أن يبدأ بـ `https://`)
2. تحقق من مفتاح service role (سلسلة JWT طويلة جداً)
3. اختبر الاتصال من سطر الأوامر:
   ```bash
   curl https://your-project-id.supabase.co/rest/v1/
   ```

### مشكلة: "Invalid password format"

**الحل:**
- يجب أن تكون كلمة المرور 8 أحرف على الأقل
- استخدم كلمة مرور قوية بأحرف كبيرة وصغيرة وأرقام ورموز
- ضع كلمة المرور بين علامات تنصيص إذا احتوت على رموز خاصة:
  ```bash
  npm run seed:superadmin -- --email="admin@test.com" --password="P@ssw0rd!"
  ```

### مشكلة: Frontend تظهر "Network Error"

**الحل:**
1. تحقق من تشغيل Backend: `curl http://localhost:3001/health`
2. تحقق من `ALLOWED_ORIGINS` في `.env.development` يتضمن رابط Frontend
3. تحقق من أن `NEXT_PUBLIC_API_URL` في Frontend يشير إلى Backend الصحيح

---

## الخطوات التالية

بعد إكمال الإعداد الأولي:

1. ✅ **سجّل الدخول** بحساب مدير النظام
2. 📋 **أنشئ مستخدمين إضافيين** من لوحة الإدارة
3. 🏢 **أنشئ المكاتب** وكوّن إعداداتها
4. 🔐 **فعّل ميزات الأمان الإضافية** (2FA، IP whitelisting)
5. 📊 **راقب السجلات** باستخدام `pm2 logs` و `pm2 monit`
6. 🔄 **كوّن النسخ الاحتياطية** لقاعدة بيانات Supabase
7. 🚀 **أنشئ خط CI/CD** للنشر التلقائي

---

## الدعم والموارد

- **التوثيق**: `/workspace/Project_Documentation/`
- **توثيق API**: `http://localhost:3001/api/docs` (التطوير فقط)
- **لوحة Supabase**: https://app.supabase.com
- **توثيق PM2**: https://pm2.keymetrics.io/docs/

---

**آخر تحديث**: 2025-11-11  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للإنتاج
